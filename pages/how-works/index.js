import Navbar from '../../components/navigation/navbar';

export default function HowWorksPage() {
    return (
        <div>
            <Navbar />

            <main>Como trabajamos pagina.</main>
        </div>
    );
}
